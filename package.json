{
  "name": "provisioner-catalog",
  "version": "0.0.63",
  "description": "STARTcloud Provisioner Catalog — the published catalog data, the private-catalog Worker and the docs, served with the pinned STARTcloud UI.",
  "startcloudUiVersion": "0.1.1",
  "private": true,
  "scripts": {
    "lint:md": "markdownlint '**/*.md' --ignore node_modules --ignore worker/node_modules --ignore vendor --ignore _site --ignore ui --ignore CHANGELOG.md",
    "lint:all": "npm run lint:md",
    "lint": "npm run lint:all",
    "lint:fix:md": "markdownlint '**/*.md' --ignore node_modules --ignore worker/node_modules --ignore vendor --ignore _site --ignore ui --ignore CHANGELOG.md --fix",
    "lint:fix:all": "npm run lint:fix:md",
    "format:check": "prettier --check .",
    "format:fix": "prettier --write .",
    "quality": "npm run lint && npm run format:check",
    "fix": "npm run lint:fix:all && npm run format:fix"
  },
  "devDependencies": {
    "markdownlint-cli": "^0.49.1",
    "prettier": "^3.9.5"
  },
  "author": "STARTcloud",
  "license": "GPL-3.0"
}
