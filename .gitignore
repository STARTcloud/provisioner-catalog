# Generated catalog data — a build artifact published to GitHub Pages by the
# data job. NEVER committed.
catalog.json
dist/

# Cloudflare Worker (the /private/* gate) — kept local by choice, deployed by
# hand with wrangler. See the plan: code never committed here.
worker/

# Node (web/ SPA)
node_modules/

# Python
__pycache__/
*.pyc

# Editors / OS
.vscode/
.settings/
.DS_Store
*.swp

# AI working files
.claude
